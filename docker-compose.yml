services:
  genai-learning-content-generator:
    build: .
    container_name: genai-learning-content-generator

    ports:
      - "8501:8501"

    env_file:
      - .env

    restart: unless-stopped